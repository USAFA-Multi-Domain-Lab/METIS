describe('API v1 - Login Suite', () => {
  let { server } = integration

  beforeAll(async () => {})

  describe('Login', () => {
    it('Should pass simple assertion', () => {
      expect(true).toBe(true)
    })
  })
})
