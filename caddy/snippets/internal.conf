# SSL_MODE=internal
#
# Certificates are issued by Caddy's own local certificate authority, for deployments
# with no public DNS. Clients show a warning until they trust that authority, whose root
# certificate can be copied out of the container from
# /data/caddy/pki/authorities/local/root.crt.
tls internal
